import * as types from './../constants/actionTypes';

export const add = () => ({ type: types.ADD });
export const reset = () => ({ type: types.RESET });

export const setNoteData = () => ({ type: types.SET_NOTE_DATA });
